{
  "name": "quicio-foundry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "quicio": "./dist/quicio.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "biome check . && tsc --noEmit",
    "test": "vitest run",
    "build": "tsup",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.6",
    "@types/node": "26.1.2",
    "tsup": "8.5.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
